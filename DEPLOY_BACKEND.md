# 🚀 Деплой Backend на облачный сервис

## Вариант 1: Railway (Рекомендуется) ⭐

### Преимущества:
- ✅ Бесплатный план (500 часов/месяц)
- ✅ Автоматический деплой из GitHub
- ✅ Простая настройка
- ✅ Автоматический HTTPS
- ✅ Переменные окружения

### Шаги:

1. **Зарегистрируйтесь на Railway**
   - Перейдите на https://railway.app
   - Войдите через GitHub

2. **Создайте новый проект**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий `telegram-logistics-app`

3. **Настройте деплой**
   - Railway автоматически определит Node.js проект
   - Укажите:
     - **Root Directory**: `server`
     - **Start Command**: `npm start`
     - **Build Command**: `npm install`

4. **Настройте переменные окружения** (опционально)
   - `PORT` - Railway установит автоматически
   - `NODE_ENV=production`

5. **Получите URL**
   - Railway предоставит URL типа: `https://your-app.railway.app`
   - Скопируйте этот URL

6. **Обновите frontend**
   - Создайте `.env` файл:
     ```env
     VITE_API_URL=https://your-app.railway.app/api
     ```
   - Или обновите `src/services/api.ts`:
     ```typescript
     const API_BASE_URL = 'https://your-app.railway.app/api';
     ```

---

## Вариант 2: Render

### Преимущества:
- ✅ Бесплатный план
- ✅ Автоматический деплой из GitHub
- ✅ Простая настройка

### Шаги:

1. **Зарегистрируйтесь на Render**
   - Перейдите на https://render.com
   - Войдите через GitHub

2. **Создайте новый Web Service**
   - Нажмите "New +" → "Web Service"
   - Подключите ваш GitHub репозиторий

3. **Настройте сервис**
   - **Name**: `telegram-logistics-api`
   - **Root Directory**: `server`
   - **Environment**: `Node`
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`

4. **Настройте переменные окружения**
   - `NODE_ENV=production`
   - `PORT` - Render установит автоматически

5. **Получите URL**
   - Render предоставит URL: `https://telegram-logistics-api.onrender.com`
   - Обновите `.env` в frontend

---

## Вариант 3: Fly.io

### Преимущества:
- ✅ Бесплатный план
- ✅ Глобальная сеть
- ✅ Быстрая работа

### Шаги:

1. **Установите Fly CLI**
   ```bash
   # Windows
   powershell -Command "iwr https://fly.io/install.ps1 -useb | iex"
   ```

2. **Войдите в Fly.io**
   ```bash
   fly auth login
   ```

3. **Создайте приложение**
   ```bash
   cd server
   fly launch
   ```

4. **Деплой**
   ```bash
   fly deploy
   ```

---

## Вариант 4: Supabase (Только база данных)

Если хотите использовать только базу данных без сервера:

1. Создайте проект на https://supabase.com
2. Используйте PostgreSQL вместо SQLite
3. Обновите код для работы с Supabase

---

## После деплоя:

1. **Проверьте работу API**
   - Откройте: `https://your-app.railway.app/api/health`
   - Должен вернуться: `{"status":"ok","timestamp":"..."}`

2. **Обновите frontend**
   - Установите переменную окружения `VITE_API_URL`
   - Или обновите код напрямую

3. **Проверьте CORS**
   - Убедитесь, что ваш frontend домен разрешен в CORS
   - В `server/server.js` уже настроен `cors()` для всех доменов

---

## Обновление кода для продакшена:

В `server/server.js` уже настроено:
- ✅ CORS для всех доменов
- ✅ Обработка ошибок
- ✅ Graceful shutdown
- ✅ Порт из переменной окружения

## Мониторинг:

- Railway: встроенный мониторинг в дашборде
- Render: логи доступны в дашборде
- Fly.io: `fly logs`

---

## Рекомендация:

**Используйте Railway** - самый простой вариант для начала! 🚀
