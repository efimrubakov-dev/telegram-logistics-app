# 📋 Пошаговая инструкция: Деплой на Render

## ✅ Шаг 1: Регистрация

1. Откройте https://render.com
2. Нажмите **"Get Started for Free"**
3. Выберите **"Sign up with GitHub"**
4. Разрешите доступ Render к вашим репозиториям

---

## ✅ Шаг 2: Создание Web Service

1. В дашборде Render нажмите **"New +"** (вверху справа)
2. Выберите **"Web Service"**
3. Если репозиторий не виден:
   - Нажмите **"Configure account"**
   - Дайте доступ к репозиториям
4. Выберите репозиторий: **`efimrubakov-dev/telegram-logistics-app`**
5. Нажмите **"Connect"**

---

## ✅ Шаг 3: Настройка сервиса

Заполните следующие поля:

| Поле | Значение |
|------|----------|
| **Name** | `telegram-logistics-api` |
| **Region** | Выберите ближайший (например, Frankfurt) |
| **Branch** | `main` |
| **Root Directory** | `server` ⚠️ **ВАЖНО!** |
| **Runtime** | `Node` |
| **Build Command** | `npm install` |
| **Start Command** | `npm start` |

---

## ✅ Шаг 4: Переменные окружения

В разделе **"Environment Variables"** добавьте:

- **Key**: `NODE_ENV`
- **Value**: `production`

**Примечание**: `PORT` устанавливается автоматически, добавлять не нужно!

---

## ✅ Шаг 5: Создание и деплой

1. Прокрутите вниз
2. Нажмите **"Create Web Service"**
3. Render начнет деплой (обычно 2-5 минут)
4. Дождитесь статуса **"Live"** (зеленая галочка)

---

## ✅ Шаг 6: Получение URL

После успешного деплоя Render покажет URL вашего сервиса:

```
https://telegram-logistics-api.onrender.com
```

**Скопируйте этот URL!** Он понадобится для настройки frontend.

---

## ✅ Шаг 7: Проверка работы

Откройте в браузере:

```
https://your-app-name.onrender.com/api/health
```

Должен вернуться JSON:
```json
{"status":"ok","timestamp":"2024-01-11T..."}
```

Если видите этот ответ - **все работает!** ✅

---

## ✅ Шаг 8: Обновление frontend

После получения URL от Render:

1. Откройте файл `src/services/api.ts`
2. Найдите строку:
   ```typescript
   const API_BASE_URL = 'https://your-app-name.onrender.com/api';
   ```
3. Замените `your-app-name` на ваш реальный URL от Render
4. Сохраните файл
5. Закоммитьте и запушьте изменения:
   ```bash
   git add src/services/api.ts
   git commit -m "Update API URL to Render"
   git push
   ```

---

## ✅ Шаг 9: Проверка в приложении

1. Подождите 2-3 минуты, пока GitHub Actions задеплоит frontend
2. Откройте приложение в Telegram
3. Приложение автоматически подключится к вашему Render API
4. Проверьте работу: создайте заказ или получателя - данные должны сохраняться в базе данных на Render

---

## 🆘 Если что-то пошло не так:

### Проблема: Деплой не запускается
- Проверьте, что Root Directory указан как `server`
- Проверьте, что Start Command: `npm start`

### Проблема: Ошибка при сборке
- Проверьте логи в Render (вкладка "Logs")
- Убедитесь, что все зависимости указаны в `server/package.json`

### Проблема: API не отвечает
- Проверьте URL: должен быть `/api/health` в конце
- Проверьте логи в Render
- Убедитесь, что сервис имеет статус "Live"

### Проблема: CORS ошибки
- В `server/server.js` уже настроен CORS для всех доменов
- Если проблема остается, проверьте логи

---

## 📝 Что дальше?

После успешного деплоя:
1. ✅ Backend работает на Render
2. ✅ Frontend работает на GitHub Pages
3. ✅ Данные сохраняются в SQLite базе на Render
4. ✅ Все синхронизировано и работает!

---

## 💡 Полезные ссылки:

- Render Dashboard: https://dashboard.render.com
- Документация Render: https://render.com/docs
- Логи вашего сервиса: доступны в дашборде Render

---

**Удачи с деплоем! Если возникнут вопросы - пишите! 🚀**
