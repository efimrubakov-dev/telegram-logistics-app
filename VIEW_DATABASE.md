# 📊 Как просматривать данные в базе данных

Есть несколько способов просмотреть данные в вашей SQLite базе данных на Render:

## Способ 1: Через браузер (Самый простой) ⭐

Я добавил специальные админ-роуты для просмотра данных. Просто откройте в браузере:

### Статистика (общее количество записей):
```
https://telegram-logistics-app.onrender.com/admin/stats
```

### Все пользователи:
```
https://telegram-logistics-app.onrender.com/admin/users
```

### Все получатели:
```
https://telegram-logistics-app.onrender.com/admin/recipients
```

### Все заказы:
```
https://telegram-logistics-app.onrender.com/admin/orders
```

### Все объединения:
```
https://telegram-logistics-app.onrender.com/admin/consolidations
```

### Все адреса доставки:
```
https://telegram-logistics-app.onrender.com/admin/delivery-addresses
```

**Примечание**: Эти роуты показывают данные в формате JSON. Для удобного просмотра используйте расширение браузера для форматирования JSON (например, JSON Formatter для Chrome).

---

## Способ 2: Через Render Shell (Продвинутый)

### Шаг 1: Откройте Shell в Render

1. Зайдите в Render Dashboard
2. Откройте ваш сервис `telegram-logistics-app`
3. В левом меню найдите "Shell" (с иконкой молнии ⚡)
4. Нажмите на "Shell"

### Шаг 2: Подключитесь к базе данных

В открывшемся терминале выполните:

```bash
cd server
sqlite3 database.sqlite
```

### Шаг 3: Выполните SQL запросы

Теперь вы можете выполнять SQL команды:

```sql
-- Показать все таблицы
.tables

-- Показать всех пользователей
SELECT * FROM users;

-- Показать всех получателей
SELECT * FROM recipients;

-- Показать все заказы
SELECT * FROM orders;

-- Показать статистику
SELECT 
  (SELECT COUNT(*) FROM users) as users,
  (SELECT COUNT(*) FROM recipients) as recipients,
  (SELECT COUNT(*) FROM orders) as orders;

-- Выход из SQLite
.exit
```

---

## Способ 3: Скачать базу данных и открыть локально

### Шаг 1: Скачайте базу данных через Render Shell

1. Откройте Shell в Render
2. Выполните:
```bash
cd server
cat database.sqlite > /tmp/database.sqlite
```

Затем скачайте файл через интерфейс Render или используйте команду для экспорта.

### Шаг 2: Откройте локально

Используйте любой SQLite просмотрщик:

**Windows:**
- DB Browser for SQLite: https://sqlitebrowser.org/
- SQLite Expert: http://www.sqliteexpert.com/

**Онлайн:**
- https://sqliteviewer.app/
- https://inloop.github.io/sqlite-viewer/

---

## Способ 4: Создать простой веб-интерфейс (Опционально)

Можно создать простую HTML страницу для просмотра данных. Если нужно - скажите, создам!

---

## 🔒 Безопасность

**Важно**: Админ-роуты сейчас открыты для всех. В продакшене добавьте:

1. Аутентификацию (пароль или токен)
2. Ограничение доступа по IP
3. Или удалите эти роуты после отладки

---

## 💡 Рекомендация

Для быстрого просмотра используйте **Способ 1** (через браузер) - это самый простой и быстрый способ!
